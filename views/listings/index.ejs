<% layout("/layouts/boilerplate") %>

<style>
  #filters {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
  }
  .filter {
    text-align: center;
    margin-right: 2rem;
    margin-top: 2rem;
    opacity: 0.7;
    text-decoration: none;
    color: inherit;
  }
  .filter:hover {
    opacity: 1;
    cursor: pointer;
  }
  .filter p {
    font-size: 0.8rem;
  }
  .tax-info {
    display: none;
  }
  .tax-toggle {
    border: 1px solid black;
    border-radius: 1rem;
    height: 3.5rem;
    padding: 1rem;
    margin-left: 8rem;
    display: flex;
    align-items: center;
  }
</style>

<div id="filters">
  <a href="/listings?category=Adventure" class="filter">
    <div><i class="fa-solid fa-fire"></i></div>
    <p>Adventure</p>
  </a>
  <a href="/listings?category=Beach" class="filter">
    <div><i class="fa-solid fa-person-swimming"></i></div>
    <p>Beach</p>
  </a>
  <a href="/listings?category=Luxury" class="filter">
    <div><i class="fa-regular fa-gem"></i></div>
    <p>Luxury</p>
  </a>
  <a href="/listings?category=Budget" class="filter">
    <div><i class="fa-solid fa-hand-holding-dollar"></i></div>
    <p>Budget</p>
  </a>
  <a href="/listings?category=Romantic" class="filter">
    <div><i class="fa-brands fa-gratipay"></i></div>
    <p>Romantic</p>
  </a>
  <a href="/listings?category=Mountain" class="filter">
    <div><i class="fa-solid fa-mountain"></i></div>
    <p>Mountain</p>
  </a>
  <a href="/listings?category=Castle" class="filter">
    <div><i class="fa-brands fa-fort-awesome"></i></div>
    <p>Castle</p>
  </a>
  <a href="/listings?category=Temple" class="filter">
    <div><i class="fa-solid fa-gopuram"></i></div>
    <p>Temple</p>
  </a>
  <a href="/listings?category=Forest" class="filter">
    <div><i class="fa-solid fa-tree"></i></div>
    <p>Forest</p>
  </a>
  <a href="/listings?category=City" class="filter">
    <div><i class="fa-solid fa-city"></i></div>
    <p>City</p>
  </a>

  <div class="tax-toggle">
    <div class="form-check-reverse form-switch">
      <input class="form-check-input" type="checkbox" role="switch" id="switchCheckDefault">
      <label class="form-check-label" for="switchCheckDefault">Display total after taxes</label>
    </div>
  </div>
</div>

<div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-3 mt-3">
  <% for (let listing of allListings) { %>
    <a href="/listings/<%= listing._id %>" class="listing-link">
      <div class="card col listing-card">
        <img src="<%= listing.image.url %>" class="card-img-top" alt="listing_image" style="height: 20rem;">
        <div class="card-img-overlay"></div>
        <div class="card-body">
          <p class="card-text">
            <b><%= listing.title %></b> <br>
            &#8377;<%= listing.price.toLocaleString("en-IN") %> / night
            <i class="tax-info"> &nbsp; &nbsp; +18% GST</i>
          </p>
        </div>
      </div>
    </a>
  <% } %>
</div>
<!-- Chatbot UI -->
<div id="chat-container" style="position: fixed; bottom: 20px; right: 20px; width: 300px; background: white; border: 1px solid #ccc; border-radius: 10px; box-shadow: 0 0 10px #ccc;">
  <div id="chat-box" style="height: 200px; overflow-y: auto; padding: 10px;"></div>
  <input type="text" id="chat-input" placeholder="Ask about listings..." style="width: 100%; border: none; border-top: 1px solid #ccc; padding: 10px;">
</div>

<script>
  const input = document.getElementById("chat-input");
  const box = document.getElementById("chat-box");

  input.addEventListener("keypress", async (e) => {
    if (e.key === "Enter" && input.value.trim() !== "") {
      const msg = input.value;
      box.innerHTML += `<div><b>You:</b> ${msg}</div>`;
      input.value = "";

      const res = await fetch("/chatbot", {
        method: "POST",
        headers: {
          "Content-Type": "application/json"
        },
        body: JSON.stringify({ message: msg })
      });
      const data = await res.json();
      box.innerHTML += `<div><b>Bot:</b> ${data.reply}</div>`;
      box.scrollTop = box.scrollHeight;
    }
  });
</script>


<script>
  let taxSwitch = document.getElementById("switchCheckDefault");
  taxSwitch.addEventListener("click", () => {
    let taxInfo = document.getElementsByClassName("tax-info");
    for (info of taxInfo) {
      if (info.style.display != "inline") {
        info.style.display = "inline";
      } else {
        info.style.display = "none";
      }
    }
  });
</script>
